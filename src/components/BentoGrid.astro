---
---

<div class="container mx-auto px-2 py-8 max-w-6xl sm:px-6 lg:px-8">
  <div class="bento-grid">
    <!-- Hero/Intro Section -->
    <div class="bento-item hero">
      <slot name="hero" />
    </div>

    <!-- Availability Widget -->
    <div class="bento-item availability">
      <slot name="availability" />
    </div>

    <!-- Command Palette Hint -->
    <div class="bento-item command-hint">
      <slot name="command-hint" />
    </div>

    <!-- Current Project -->
    <div class="bento-item current-project">
      <slot name="current-project" />
    </div>

    <!-- GitHub Stats -->
    <div class="bento-item github-stats">
      <slot name="github-stats" />
    </div>

    <!-- Tech Stack -->
    <div class="bento-item tech-stack">
      <slot name="tech-stack" />
    </div>

    <!-- Recent Posts -->
    <div class="bento-item recent-posts">
      <slot name="recent-posts" />
    </div>

    <!-- Metrics -->
    <div class="bento-item metrics">
      <slot name="metrics" />
    </div>

    <!-- Healthcare Expertise -->
    <div class="bento-item healthcare-expertise">
      <slot name="healthcare-expertise" />
    </div>

    <!-- AI Expertise -->
    <div class="bento-item ai-expertise">
      <slot name="ai-expertise" />
    </div>

    <!-- Security Expertise -->
    <div class="bento-item security-expertise">
      <slot name="security-expertise" />
    </div>

    <!-- Certifications -->
    <div class="bento-item certifications">
      <slot name="certifications" />
    </div>

    <!-- Itihaas Heritage Platform -->
    <div class="bento-item itihaas">
      <slot name="itihaas" />
    </div>

    <!-- Testimonials -->
    <div class="bento-item testimonials">
      <slot name="testimonials" />
    </div>
  </div>
</div>

<style>
  .bento-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    grid-template-rows: auto;
    gap: 1.5rem;
    grid-template-areas:
      "hero hero hero metrics"
      "healthcare-expertise healthcare-expertise ai-expertise ai-expertise"
      "security-expertise security-expertise github-stats github-stats"
      "testimonials testimonials testimonials testimonials"
      "current-project current-project availability command-hint"
      "tech-stack tech-stack certifications certifications"
      "recent-posts recent-posts recent-posts recent-posts"
      "itihaas itihaas itihaas itihaas";
  }

  /* Light mode bento item */
  .bento-item {
    background: rgba(255, 255, 255, 0.7);
    border: 1px solid rgba(0, 0, 0, 0.08);
    border-radius: 16px;
    padding: 1.5rem;
    backdrop-filter: blur(20px);
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    overflow: hidden;
  }

  .bento-item:hover {
    border-color: rgba(139, 92, 246, 0.4);
    background: rgba(255, 255, 255, 0.9);
    transform: translateY(-4px) scale(1.01);
    box-shadow:
      0 20px 40px rgba(0, 0, 0, 0.1),
      0 0 20px rgba(139, 92, 246, 0.15);
  }

  /* Dark mode bento item */
  :global(.dark) .bento-item {
    background: rgba(30, 41, 59, 0.4);
    border: 1px solid rgba(148, 163, 184, 0.1);
  }

  :global(.dark) .bento-item:hover {
    background: rgba(30, 41, 59, 0.6);
    box-shadow:
      0 20px 40px rgba(0, 0, 0, 0.3),
      0 0 20px rgba(139, 92, 246, 0.2);
  }

  .bento-item::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(139, 92, 246, 0.05) 0%, rgba(236, 72, 153, 0.02) 50%, transparent 100%);
    opacity: 0;
    transition: opacity 0.3s ease;
    pointer-events: none;
  }

  :global(.dark) .bento-item::before {
    background: linear-gradient(135deg, rgba(139, 92, 246, 0.08) 0%, rgba(236, 72, 153, 0.04) 50%, transparent 100%);
  }

  .bento-item:hover::before {
    opacity: 1;
  }

  .bento-item::after {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(0, 0, 0, 0.02), transparent);
    transition: left 0.6s ease;
    pointer-events: none;
  }

  :global(.dark) .bento-item::after {
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.05), transparent);
  }

  .bento-item:hover::after {
    left: 100%;
  }

  .hero {
    grid-area: hero;
    min-height: 200px;
  }

  .availability {
    grid-area: availability;
    min-height: 140px;
  }

  .command-hint {
    grid-area: command-hint;
    min-height: 140px;
  }

  .current-project {
    grid-area: current-project;
    min-height: 200px;
  }

  .github-stats {
    grid-area: github-stats;
    min-height: 280px;
  }

  .tech-stack {
    grid-area: tech-stack;
    min-height: 150px;
  }

  .recent-posts {
    grid-area: recent-posts;
    min-height: 150px;
  }

  .metrics {
    grid-area: metrics;
    min-height: 120px;
  }

  .healthcare-expertise {
    grid-area: healthcare-expertise;
    min-height: 280px;
  }

  .ai-expertise {
    grid-area: ai-expertise;
    min-height: 280px;
  }

  .security-expertise {
    grid-area: security-expertise;
    min-height: 280px;
  }

  .certifications {
    grid-area: certifications;
    min-height: 140px;
  }

  .itihaas {
    grid-area: itihaas;
    min-height: 140px;
    padding: 0;
    overflow: hidden;
  }

  .testimonials {
    grid-area: testimonials;
    min-height: 400px;
  }

  /* Responsive Design */
  @media (max-width: 1200px) {
    .bento-grid {
      grid-template-columns: repeat(4, 1fr);
      gap: 1.25rem;
      grid-template-areas:
        "hero hero hero metrics"
        "healthcare-expertise healthcare-expertise ai-expertise ai-expertise"
        "security-expertise security-expertise github-stats github-stats"
        "testimonials testimonials testimonials testimonials"
        "current-project current-project availability command-hint"
        "tech-stack tech-stack certifications certifications"
        "recent-posts recent-posts recent-posts recent-posts"
        "itihaas itihaas itihaas itihaas";
    }
  }

  @media (max-width: 768px) {
    .bento-grid {
      grid-template-columns: 1fr;
      grid-template-areas:
        "hero"
        "metrics"
        "healthcare-expertise"
        "ai-expertise"
        "security-expertise"
        "github-stats"
        "availability"
        "testimonials"
        "current-project"
        "tech-stack"
        "certifications"
        "recent-posts"
        "itihaas"
        "command-hint";
      gap: 1rem;
    }

    .bento-item {
      padding: 1rem;
      min-height: auto !important;
    }

    .hero {
      min-height: 200px;
    }

    .availability {
      min-height: 180px;
    }

    .command-hint {
      min-height: 80px;
    }

    .current-project {
      min-height: 200px;
    }

    .github-stats {
      min-height: 250px;
    }

    .recent-projects {
      min-height: 300px;
    }

    .recent-posts {
      min-height: 180px;
    }

    .tech-stack {
      min-height: 120px;
    }
  }

  @media (max-width: 480px) {
    .container {
      padding-left: 0.5rem;
      padding-right: 0.5rem;
    }

    .bento-grid {
      gap: 0.5rem;
    }

    .bento-item {
      padding: 0.75rem;
      border-radius: 12px;
    }

    .hero {
      min-height: 180px;
    }

    .availability {
      min-height: 160px;
    }
  }
</style>